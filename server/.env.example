# Truco multiplayer server configuration.
# Copy to .env and adjust. All values are optional.

# Port the WebSocket relay listens on.
PORT=8443

# Optional Supabase persistence for game history/stats.
# Leave both unset to run in memory-only mode.
SUPABASE_URL=
SUPABASE_KEY=
