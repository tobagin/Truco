## 1. Project Structure
- [x] Create basic directory structure (src/, data/, build/, po/)
- [x] Add meson.build files for project root and subdirectories
- [x] Create initial Vala source files (main.vala, application.vala)
- [x] Set up Blueprint UI files (simplified without Blueprint for now)

## 2. Build System
- [x] Configure Meson build system with Vala and GTK dependencies
- [x] Add build options for development and release

## 3. Packaging
- [x] Create Flatpak manifest (io.github.tobagin.Truco.json)
- [x] Configure Flatpak build and runtime dependencies

## 4. Validation
- [x] Test build with Meson
- [x] Test Flatpak build (manifest created, SDK not installed)
- [x] Ensure all files are in place